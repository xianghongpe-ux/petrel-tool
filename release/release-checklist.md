# 海燕党 · DZN v1.0 发布检查表

> 创世铭文: 彼苍者天，照临下土。维此党员，为民之矩。
>
> 按总体方案附录逐项确认。所有项通过后方可发布。

---

## ⬜ 1. 代码完整性

- [ ] DZN 核心模块全部通过 Python 语法检查
  - [ ] `dzn_scheduler.py` — 无语法/类型错误
  - [ ] `model_consensus.py` — 无语法/类型错误
  - [ ] `ai_output_lock.py` — 无语法/类型错误
  - [ ] `inference_node.py` — 无语法/类型错误
- [ ] Docker 镜像构建成功（全部 4 个目标）
- [ ] `docker-compose.yml` YAML 语法正确
- [ ] `deploy.sh` ShellCheck 通过
- [ ] 前端页面（market, dashboard）加载正常

## ⬜ 2. 功能验证

- [ ] **调度器**: `python dzn_scheduler.py --demo` 正常启动
- [ ] **共识引擎**: `python model_consensus.py --demo` 输出正确共识结果
- [ ] **输出锁**: `python ai_output_lock.py --demo` 锁定/签名/释放流程完整
- [ ] **推理节点**: `python inference_node.py --help` 参数列表完整
- [ ] **Docker 一键起栈**: `docker compose up -d` 全部绿色
- [ ] **健康检查**: 所有 4 个服务 `/health` 返回 200
- [ ] **REST API**: `GET /p2p/ping` 返回 genesis 铭文
- [ ] **双向通信**: 投票客户端与网关握手成功

## ⬜ 3. 安全审计

- [ ] 无硬编码密钥/Token/密码
- [ ] 所有配置文件使用环境变量注入
- [ ] Docker 镜像不包含 `:latest` 标签（使用固定版本）
- [ ] 熔断器三级阈值合理（15%/30%/50%）
- [ ] 共识引擎 ≥3 节点最小约束强制执行
- [ ] 输出锁人类多签凭证字段完整
- [ ] HTTP 服务绑定 `0.0.0.0` 时权限控制检查

## ⬜ 4. 文档完整性

- [ ] 所有 Python 文件携带创世铭文（文件头注释）
- [ ] `README.md` 包含安装/运行/配置说明
- [ ] `offline-seed.md` 包含 U 盘部署全流程
- [ ] 所有 CLI 参数有 `--help` 说明
- [ ] API 端点有基本响应格式说明

## ⬜ 5. 部署验证

### Docker 模式
- [ ] `docker compose build` 无缓存构建成功
- [ ] `docker compose up -d` 服务全部 `healthy`
- [ ] `curl localhost:8765/p2p/ping` 返回 200
- [ ] 看板页面 `localhost:9103` 正常渲染
- [ ] 市场前端 `localhost:9102` 正常渲染
- [ ] Nginx 80 端口反向代理各服务正常

### 裸机模式
- [ ] `pip install -r requirements.txt` 全部成功
- [ ] `python dzn_scheduler.py` 启动无报错
- [ ] `python inference_node.py` 启动硬件检测正常
- [ ] GPU 检测（nvidia-smi）降级处理正常

### 离线模式
- [ ] U盘目录结构完整（DZN-SEED/ 标准布局）
- [ ] `offline-verify.sh` 校验通过
- [ ] 无网络依赖：所有代码/依赖本地可用

## ⬜ 6. 性能基准

- [ ] 调度器处理 ≥100 并发任务无崩溃
- [ ] 共识引擎 5 节点同步计算 < 3 秒
- [ ] 熔断器 1000 次错误记录无内存泄漏
- [ ] 推理节点 CPU 模式下内存占用 < 4GB
- [ ] Docker 启动时间（全部服务）< 60 秒

## ⬜ 7. 鲁棒性测试

- [ ] 调度器离线 → 节点心跳失败自动重试
- [ ] 节点执行超时 → 自动重新调度
- [ ] 熔断触发 → 输出被正确拦截
- [ ] 熔断恢复 → 输出恢复正常
- [ ] 共识分歧 ≥ 2 个节点 → 正确标记分歧
- [ ] 异常输入 → 返回 400 且不崩溃

## ⬜ 8. 发布准备

- [ ] 版本号更新至 `v1.0.0`（所有文件一致）
- [ ] CHANGELOG.md 编写完成
- [ ] GitHub Release 草稿已创建
- [ ] Docker Hub 镜像推送测试成功
- [ ] 协议种子 U 盘制作完成（至少 3 个拷贝）
- [ ] 社区公告草案已准备
- [ ] 灾难恢复手册已定稿

---

## 🚀 发布确认

```
发布版本: v1.0.0
发布日期: ________
发布负责人: ________
安全审核: ________
代码签名: ________
```

### 签名栏

```
负责人签字: ________________
技术审核: ________________
社区代表: ________________
```

---

> **"彼苍者天，照临下土。维此党员，为民之矩。"**
> 发布即承诺，代码即誓言。
